export interface SiteTheme {
  page: {
    background: string;
  };

  productCard: {
    outer: string;
    inner: string;
    image: string;
    button: string;
    badge: string;
  };

  staticCard: {
    outer: string;
    inner: string;
    image: string;
    badge: string;
  };
}