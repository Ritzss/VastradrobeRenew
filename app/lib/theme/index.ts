import defaultTheme from "./default";
import independenceTheme from "./independence";

export const THEMES = {
  default: defaultTheme,
  independence: independenceTheme,
};

// export const theme = THEMES.default;

// Later:
export const theme = THEMES.independence;